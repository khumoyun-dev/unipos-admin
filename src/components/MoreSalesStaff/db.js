function random() {
    return Math.floor(Math.random() * (0, 100) + 2);
}

const moreSalesStaff = {
    id: random(),
    fullname: "Ali Olimov",
    jobtitle: "Kassir",
    branch: "M. Ulug'bek filiali",
    checks: "150",
}

export default moreSalesStaff;